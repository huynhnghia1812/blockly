'use strict';

goog.provide('Blockly.Swift.type');

goog.require('Blockly.Swift');

Blockly.Swift['type_int'] = function(block) {
  // TODO: Assemble Swift into code variable.
  var code = 'Int';
  // TODO: Change ORDER_NONE to the correct strength.
  return [code, Blockly.Swift.ORDER_NONE];
};

Blockly.Swift['type_double'] = function(block) {
  // TODO: Assemble Swift into code variable.
  var code = 'Double';
  // TODO: Change ORDER_NONE to the correct strength.
  return [code, Blockly.Swift.ORDER_NONE];
};

Blockly.Swift['type_bool'] = function(block) {
  // TODO: Assemble Swift into code variable.
  var code = 'Bool';
  // TODO: Change ORDER_NONE to the correct strength.
  return [code, Blockly.Swift.ORDER_NONE];
};

Blockly.Swift['type_string'] = function(block) {
  // TODO: Assemble Swift into code variable.
  var code = 'String';
  // TODO: Change ORDER_NONE to the correct strength.
  return [code, Blockly.Swift.ORDER_NONE];
};